---
- name: Export AAP installer secret keys for safekeeping
  hosts: localhost
  connection: local
  gather_facts: true
  tasks:
    - name: Export installer-generated crypto secrets
      ansible.builtin.include_role:
        name: lennysh.aap_maintenance.aap_installer_secrets_export
      vars:
        install_type: "{{ install_type }}"
