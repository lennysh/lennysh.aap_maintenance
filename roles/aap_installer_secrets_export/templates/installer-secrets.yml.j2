# {{ ansible_managed | default('Exported by lennysh.aap_maintenance.aap_installer_secrets_export') }}
#
# Install type: {{ install_type }}
# Generated: {{ ansible_date_time.iso8601 | default('unknown') }}
#
# Usage (recommended):
#   ansible-vault encrypt {{ installer_secrets_output_basename }}.yml
#   ansible-playbook -i inventory .../install -e @{{ installer_secrets_output_basename }}.yml
#
# Copy/paste helper: see {{ installer_secrets_output_basename }}-inventory.snippet
#
{% for group_name in _installer_secrets_export.keys() | sort %}
# [{{ group_name }}:vars]
{% for var_name, var_value in _installer_secrets_export[group_name] | dictsort %}
{% if (_installer_secrets_notes[group_name] | default({}))[var_name] is defined %}
# {{ (_installer_secrets_notes[group_name] | default({}))[var_name] }}
{% endif %}
{{ var_name }}: {{ var_value | to_json }}
{% endfor %}

{% endfor %}
